# .env (React 프로젝트 루트에 위치)

# ───────────────────────────────────────────────────────────
# Django REST API 기본 URL (면접 질문 생성, 자세 전송, 피드백 생성 등)
# ───────────────────────────────────────────────────────────
VITE_API_BASE_URL=http://localhost:8000/api

# ───────────────────────────────────────────────────────────
# FastAPI WebSocket(STT) 기본 호스트
# ───────────────────────────────────────────────────────────
VITE_WS_HOST=ws://localhost:8001/ws/transcribe

# ───────────────────────────────────────────────────────────
# FastAPI REST(API) – 오디오+전사 업로드 전용 URL
#    → 로그인 후 오디오 업로드 시 이 주소로 요청을 보냅니다.
#    FastAPI 쪽 코드는 /upload 엔드포인트를 처리합니다.
# ───────────────────────────────────────────────────────────
VITE_STT_HTTP=http://localhost:8000/audio
